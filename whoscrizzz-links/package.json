{
  "name": "whoscrizzz-links",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute d1-template-database --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute d1-template-database --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.99.0"
  }
}
